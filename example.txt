generate an example of utilsiation 

Here's an example of how to utilize the script:

Step 1: Create a DeployVM.csv file

Create a new CSV file named "DeployVM.csv" with the following headers:

Code

Copier le code
Name, Boot, OSType, Template, CustSpec, Folder, ResourcePool, CPU, RAM, Disk2, Disk3, Disk4, Datastore, DiskStorageFormat, NetType, Network, DHCP, IPAddress, SubnetMask, Gateway, pDNS, sDNS, Notes
Step 2: Populate the DeployVM.csv file

Add the following data to the CSV file:

Code

Copier le code
VM1, true, Windows, Windows2012, Windows2012-CustSpec, MyFolder, MyResourcePool, 2, 4, 10, 20, , MyDatastore, Thin, vSS, MyNetwork, false, 192.168.1.100, 255.255.255.0, 192.168.1.1, 8.8.8.8, 8.8.4.4, My Notes
VM2, false, Linux, Ubuntu, Ubuntu-CustSpec, MyFolder, MyResourcePool, 1, 2, , , , MyDatastore, Thin, vSS, MyNetwork, true, , , , , , My Notes
VM3, true, Windows, Windows2012, Windows2012-CustSpec, MyFolder, MyResourcePool, 4, 8, 20, 30, , MyDatastore, Thick, vDS, MyNetwork, false, 192.168.1.101, 255.255.255.0, 192.168.1.1, 8.8.8.8, 8.8.4.4, My Notes
Step 3: Run the script

Run the script using the following command:

Code

Copier le code
.\DeployVM.ps1 -csvfile "C:\Scripts\DeployVM.csv" -vcenter "myvcenter.mydomain.com" -auto
This will deploy the three VMs specified in the DeployVM.csv file to the vCenter server "myvcenter.mydomain.com" without prompting for confirmation.

Step 4: Verify the deployment

Verify that the VMs have been deployed successfully by checking the vCenter server and the DeployVM.log file generated by the script.
